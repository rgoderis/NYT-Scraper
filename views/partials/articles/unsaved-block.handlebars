<div class="card">
    <div class="card-header">
        <h4><a href="{{link}}">{{title}}</a></h4><button class="save btn btn-primary" data-id="{{id}}" is-saved="{{saved}}">Save Article</button>
    </div>
    <div class="card-body">
        <p>{{text}}</p>
    </div>
</div>